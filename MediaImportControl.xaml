<UserControl x:Class="RAPPTest.MediaImportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
             SizeChanged="UserControl_SizeChanged" xmlns:my="clr-namespace:RAPPTest">

    <UserControl.Resources>
        <DataTemplate x:Key="ImageGalleryDataTemplate">
            <Grid>
                <!--Bind Image Path in Image Control-->
                <Image Source="{Binding ImagePath}" Stretch="Fill"  HorizontalAlignment="Center">
                    <!--View Large Image on Image Control Tooltip-->
                    <Image.ToolTip>
                        <Grid>
                            <Image Source="{Binding ImagePath}" Stretch="Fill" HorizontalAlignment="Center" Height="200" Width="200"></Image>
                        </Grid>
                    </Image.ToolTip>
                </Image>
            </Grid>
        </DataTemplate>
        <ItemsPanelTemplate x:Key="ImageGalleryItemsPanelTemplate">
            <!--Display Images on UniformGrid Panel-->
            <UniformGrid Columns="4" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
        </ItemsPanelTemplate>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="dndSrollbar" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <ListBox Grid.Row="1" x:Name="lstImageGallery"  ItemsSource="{Binding}" 
                 telerik:RadDragAndDropManager.AllowDrop="True"
                 telerik:RadDragAndDropManager.DropQuery="OnDropQuery"
                 telerik:RadDragAndDropManager.DropInfo="OnDropInfo" BorderThickness="0" Background="Transparent">
            </ListBox>
        </Grid>
    </ScrollViewer>
</UserControl>
